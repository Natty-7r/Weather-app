/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "a.hecdn.net",
      "cdn.luxe.digital",
      "images.unsplash.com",
      "cdn-images.article.com",
      "images.pexels.com",
      "img.freepik.com",
    ],
  },
};

export default nextConfig;
